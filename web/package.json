{
  "name": "message-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:raw": "cp -r src dist",
    "preview": "wrangler pages dev ./src"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
